namespace aoc2021
{
    internal class DayX : IAocTask
    {
        public long Task1(string indatafile)
        {
            var indata = File.ReadAllLines(indatafile).Select(l => l).ToArray();
            return 0;
        }

        public long Task2(string indatafile)
        {
            var indata = File.ReadAllLines(indatafile).Select(l => l).ToArray();
            return 0;
        }
    }
}
